program assignments;
    var x, y : integer;
    var aashnaIsLate : integer;
    var z : real;
begin
    x := 20 + 20;
    y := 55;
    writeln(x, x);
    writeln(y);
    aashnaIsLate := 4-2;
    writeln(aashnaIsLate);
end.
