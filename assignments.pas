program assignments;
var
    x, y : integer;
var
    aashnaIsLate : integer;
var
    aChar : char;
begin
    x := 20 + 20;
    y := 55;
    aChar := 'a';
    writeln(x, x);
    writeln(y);
    aashnaIsLate := 5 + 2 * 10 / 2 * 2 + 1 - 6;
    writeln(aashnaIsLate);
    writeln(aChar);
end.
