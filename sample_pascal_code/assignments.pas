program assignments;
var
    x, y : integer;
var
    hello : integer;
var
    aChar : char;
begin
    x := 20 + 20;
    y := (5 + 5) * 2;
    aChar := 'a';
    writeln(x, x);
    writeln(y);
    hello := 5 + 2 * 10 / 2 * 2 + 1 - 6;
    writeln(hello);
    writeln(aChar);
end.
