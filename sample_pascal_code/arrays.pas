program arrays;

var i, x : integer;
var avg : real;

begin
    x := 55;
    avg := 5.0;
    // Example from http://www.tutorialspoint.com/pascal/pascal_arrays.htm
    for i := 0 to 10 do
        begin
//            a[i] := i;
//            avg := avg + a[i];
            writeln(avg);
        end;
end.
